import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
  },
  pdf: {
    width: '100%',
    height: '100%',
  },
});

export {styles};
